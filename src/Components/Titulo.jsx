import { Card } from "react-bootstrap";

const Titulo = () => {
    return (
        <Card body className='rounded-4 bg-success-subtle text-center'>
            <h1>FORMULARIO</h1>
        </Card>
    );
};

export default Titulo;